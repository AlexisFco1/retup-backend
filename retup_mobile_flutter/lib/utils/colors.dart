import 'package:flutter/material.dart';

class AppColors {
  // Primarios (RetUp brand)
  static const Color primaryColor = Color(0xFF6366F1); // Índigo
  static const Color secondaryColor = Color(0xFF10B981); // Verde
  static const Color accentColor = Color(0xFFF59E0B); // Ámbar

  // Neutrales
  static const Color white = Color(0xFFFFFFFF);
  static const Color black = Color(0xFF000000);
  static const Color grey = Color(0xFF9CA3AF);
  static const Color lightGrey = Color(0xFFF3F4F6);
  static const Color darkGrey = Color(0xFF374151);

  // Estados
  static const Color success = Color(0xFF10B981);
  static const Color error = Color(0xFFEF4444);
  static const Color warning = Color(0xFFF59E0B);
  static const Color info = Color(0xFF3B82F6);

  // Gamificación
  static const Color streak = Color(0xFFFF6B6B); // Rojo para rachas
  static const Color xp = Color(0xFFA78BFA); // Púrpura para XP
  static const Color badge = Color(0xFFFCD34D); // Amarillo para insignias
}
